  // O(n)
for(let i = 1; i < n; i++) {
  if (true) {}
}

// O(n^2)
for(let i = 1; i < n; i++) {
  for (let j = 1; j < n; i++) {
    if (true) {}
  }
}

// O(n^2)
array.map((v) => {
  if (array2.includes(v))
    return v;
})

// O(n);
let res = 1;
if (array2.includes(v))
    res = v;

array.map((v) => {
  res
})
